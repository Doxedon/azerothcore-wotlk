name: Build and Publish

on:
  push:
    branches: [ "Playerbot" ]
  workflow_dispatch: # This line allows you to manually trigger the workflow from the Actions tab!

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 1. Database Importer (Required to initialize SQL on first run)
      - name: Build and push db-import
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/docker/Dockerfile
          target: db-import
          push: true
          tags: ghcr.io/doxedon/ac-wotlk-db-import:playerbot

      # 2. Worldserver Image
      - name: Build and push Worldserver
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/docker/Dockerfile
          target: worldserver
          push: true
          tags: ghcr.io/doxedon/ac-wotlk-worldserver:playerbot

      # 3. Authserver Image
      - name: Build and push Authserver
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/docker/Dockerfile
          target: authserver
          push: true
          tags: ghcr.io/doxedon/ac-wotlk-authserver:playerbot
